# build artifacts (regenerable: python run.py / python -m knowledge_os.ingest)
knowledge_os.db
corpus.db
corpus.db-wal
corpus.db-shm
corpus_test.db*
__pycache__/
*.pyc
.DS_Store

# frontend (app/)
app/node_modules/
app/dist/

# design-process screenshots (not source)
design/*.png

# NOTE: app/public/data/ is intentionally committed — the static site needs it
# to build and deploy without Python or corpus.db present.

.vercel
